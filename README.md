

ѕрограмма УWindow ChatФ представл€ет собой симул€тор чата, упрощенную реализацию (имитацию) чата в одном процессе операционной системы. —етевое взаимодействие и хранение сообщений имитируетс€ через пам€ть процесса. ”частники - это просто отдельные окна программы.

ƒл€ сборки программы необходимы следующие зависимости:

- библиотека Qt версии 5.12.x (https://download.qt.io/archive/qt/5.12);
- система сборки Cmake версии 3.5 и выше;
- компил€тор C++ с поддержкой стандарта 11.

ѕеред сборкой необходимо убедитьс€, что в переменную PATH добавлены пути дл€ CMake и Qt. Ќапример, если Qt установлена в директорию C:\Qt, то PATH должна иметь значени€: C:\Qt\Qt5.12.7\5.12.7\mingw73_32\bin; C:\Qt\Qt5.12.7\Tools\mingw730_32\bin.

—борка: ¬ажно! ѕуть к исходному коду не должен содержать кириллицу. ќткрыть командную строку/терминал и перейти в каталог с исходным кодом. ¬ыполнить последовательно команды ниже без кавычек (пример дл€ MinGW, в ќ— Windows):

Уmkdir buildФ  
Уcd buildФ  
Уcmake -G "MinGW Makefiles" -D CMAKE_BUILD_TYPE=Release ..Ф  
Уmingw32-makeФ

(генерацию Makefile дл€ других компил€торов см. в справке cmake Цhelp, описание ключа -G).  
ѕосле успешной сборки р€дом с исходным кодом в каталоге build по€витс€ исполн€емый файл УWindowChat.exeФ.